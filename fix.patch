diff --git a/dashboard.html b/dashboard.html
index cf640af..ccb8e97 100644
--- a/dashboard.html
+++ b/dashboard.html
@@ -442,6 +442,7 @@
             text-align: center;
             cursor: pointer;
             transition: all var(--transition);
+            position: relative;
         }
         .auto-type-card:hover { border-color: var(--primary); transform: translateY(-3px); box-shadow: var(--shadow); }
         .auto-type-card.selected { border-color: var(--primary); background: #eff6ff; }
@@ -887,25 +888,23 @@
                         <p class="text-muted mb-2">Select which platform(s) this automation will run on.</p>
                         <div class="auto-type-grid" id="platformSelectGrid">
                             <div class="auto-type-card selected" data-platform="instagram">
+                                <input type="checkbox" class="platform-checkbox" checked style="position:absolute;top:10px;right:10px;width:16px;height:16px;pointer-events:none;">
                                 <div class="icon"><i class="fab fa-instagram"></i></div>
                                 <h4>Instagram</h4>
                                 <p>Comments and DMs on Instagram</p>
                             </div>
                             <div class="auto-type-card" data-platform="facebook">
+                                <input type="checkbox" class="platform-checkbox" style="position:absolute;top:10px;right:10px;width:16px;height:16px;pointer-events:none;">
                                 <div class="icon"><i class="fab fa-facebook-f"></i></div>
                                 <h4>Facebook</h4>
                                 <p>Comments and DMs on Facebook Pages</p>
                             </div>
                             <div class="auto-type-card" data-platform="threads">
+                                <input type="checkbox" class="platform-checkbox" style="position:absolute;top:10px;right:10px;width:16px;height:16px;pointer-events:none;">
                                 <div class="icon"><i class="fas fa-at"></i></div>
                                 <h4>Threads</h4>
                                 <p>Replies on Threads</p>
                             </div>
-                            <div class="auto-type-card" data-platform="all">
-                                <div class="icon"><i class="fas fa-globe"></i></div>
-                                <h4>All Platforms</h4>
-                                <p>Run on all connected platforms</p>
-                            </div>
                         </div>
                         <div class="flex" style="justify-content:flex-end; margin-top:16px;">
                             <button class="btn btn-primary step-next">Next: Scope <i class="fas fa-arrow-right"></i></button>
@@ -1601,7 +1600,11 @@
         document.getElementById('kwInput').value = '';
         document.querySelectorAll('.variation-input textarea').forEach((ta, i) => { if (i > 0) ta.closest('.variation-input').remove(); else ta.value = ''; });
         document.querySelectorAll('.auto-type-card').forEach(c => c.classList.remove('selected'));
-        document.querySelector('.auto-type-card[data-platform="instagram"]').classList.add('selected');
+        document.querySelectorAll('#platformSelectGrid .platform-checkbox').forEach(cb => { cb.checked = false; });
+        const instagramCard = document.querySelector('.auto-type-card[data-platform="instagram"]');
+        instagramCard.classList.add('selected');
+        const instagramCheckbox = instagramCard.querySelector('.platform-checkbox');
+        if (instagramCheckbox) instagramCheckbox.checked = true;
         document.querySelectorAll('#scopeSelectGrid .auto-type-card').forEach(c => c.classList.remove('selected'));
         document.querySelector('#scopeSelectGrid .auto-type-card[data-scope="all"]').classList.add('selected');
         document.getElementById('specificPostSelector').style.display = 'none';
@@ -1616,11 +1619,27 @@
         autoList.style.display = 'block';
     });
 
-    // Platform selection
+    // Platform selection — multi-select (checkbox behavior): clicking toggles
+    // a card on/off rather than exclusively selecting one. At least one
+    // platform must stay selected at all times.
+    function getSelectedPlatforms() {
+        return Array.from(document.querySelectorAll('#platformSelectGrid .auto-type-card.selected'))
+            .map(c => c.dataset.platform);
+    }
     document.querySelectorAll('#platformSelectGrid .auto-type-card').forEach(card => {
         card.addEventListener('click', function() {
-            document.querySelectorAll('#platformSelectGrid .auto-type-card').forEach(c => c.classList.remove('selected'));
-            this.classList.add('selected');
+            const willBeSelected = !this.classList.contains('selected');
+            if (!willBeSelected && getSelectedPlatforms().length === 1) {
+                // Don't allow deselecting the last remaining platform
+                return;
+            }
+            this.classList.toggle('selected');
+            const checkbox = this.querySelector('.platform-checkbox');
+            if (checkbox) checkbox.checked = this.classList.contains('selected');
+            // If the post picker (step 2) is already showing, its list of
+            // candidate posts depends on which platforms are selected here —
+            // re-filter it so it stays in sync.
+            if (postsLoadedForAutomation) renderPostRadioOptions();
         });
     });
 
@@ -1631,16 +1650,34 @@
 
     function renderPostRadioOptions() {
         const list = document.getElementById('postSelectList');
+        const selectedPlatforms = getSelectedPlatforms();
+
         // Local posts (created/published through this app) plus remote posts
         // (published directly on Instagram/Facebook/Threads) that aren't
         // already tracked locally — those get a "remote:" prefixed value.
-        const untrackedRemote = remotePostsForAutomation.filter(rp => !rp.local_post_id);
-        if (!posts.length && !untrackedRemote.length) {
-            list.innerHTML = '<div class="text-muted" style="padding:8px;">No posts found yet. Create a post first, then come back here to target it.</div>';
+        // Both are filtered down to whichever platform(s) are checked in step 1 —
+        // a post only counts if it went out on at least one of those platforms.
+        const filteredLocal = posts.filter(p => (p.platforms || []).some(pl => selectedPlatforms.includes(pl)));
+        const untrackedRemote = remotePostsForAutomation
+            .filter(rp => !rp.local_post_id)
+            .filter(rp => selectedPlatforms.includes(rp.platform));
+
+        // If the previously-selected post no longer matches the current
+        // platform filter, drop the selection rather than silently keep it.
+        if (selectedAutomationPostId) {
+            const stillLocalMatch = filteredLocal.some(p => String(p.id) === String(selectedAutomationPostId));
+            const stillRemoteMatch = typeof selectedAutomationPostId === 'string' && selectedAutomationPostId.startsWith('remote:') &&
+                untrackedRemote.some(rp => `remote:${rp.platform}:${rp.remote_id}` === selectedAutomationPostId);
+            if (!stillLocalMatch && !stillRemoteMatch) selectedAutomationPostId = null;
+        }
+
+        if (!filteredLocal.length && !untrackedRemote.length) {
+            const platformNames = selectedPlatforms.map(p => p.charAt(0).toUpperCase() + p.slice(1)).join('/');
+            list.innerHTML = `<div class="text-muted" style="padding:8px;">No posts found on ${platformNames} yet. Create or publish a post there first, then come back here to target it.</div>`;
             return;
         }
 
-        const localRows = [...posts]
+        const localRows = [...filteredLocal]
             .sort((a, b) => new Date(b.scheduled_date || 0) - new Date(a.scheduled_date || 0))
             .map(p => {
                 const platformLabel = (p.platforms || []).map(pl => pl.charAt(0).toUpperCase() + pl.slice(1)).join(', ') || 'No platform';
@@ -1777,8 +1814,7 @@
     // Update review summary
     function updateReview() {
         const name = document.getElementById('autoNameInput').value || '(not set)';
-        const platformCard = document.querySelector('#platformSelectGrid .auto-type-card.selected');
-        const platform = platformCard ? platformCard.dataset.platform : 'all';
+        const selectedPlatformsForReview = getSelectedPlatforms();
         const scopeCard = document.querySelector('#scopeSelectGrid .auto-type-card.selected');
         const scope = scopeCard ? scopeCard.dataset.scope : 'all';
         const trigger = document.getElementById('autoTrigger').value;
@@ -1787,7 +1823,8 @@
         const replyLocation = document.getElementById('replyLocation').value;
 
         document.getElementById('reviewName').textContent = name;
-        document.getElementById('reviewPlatform').textContent = platform.charAt(0).toUpperCase() + platform.slice(1);
+        document.getElementById('reviewPlatform').textContent = selectedPlatformsForReview
+            .map(p => p.charAt(0).toUpperCase() + p.slice(1)).join(', ') || '(none selected)';
         if (scope === 'all') {
             document.getElementById('reviewScope').textContent = 'All Future Posts';
         } else if (typeof selectedAutomationPostId === 'string' && selectedAutomationPostId.startsWith('remote:')) {
@@ -1809,9 +1846,12 @@
         const name = document.getElementById('autoNameInput').value.trim();
         if (!name) { alert('Please give this automation a name.'); showStep(1); return; }
         
-        const platformCard = document.querySelector('#platformSelectGrid .auto-type-card.selected');
-        const platform = platformCard ? platformCard.dataset.platform : 'instagram';
-        const platforms = platform === 'all' ? ['instagram', 'facebook', 'threads'] : [platform];
+        const platforms = getSelectedPlatforms();
+        if (!platforms.length) {
+            alert('Please select at least one platform.');
+            showStep(1);
+            return;
+        }
 
         const scopeCardForSubmit = document.querySelector('#scopeSelectGrid .auto-type-card.selected');
         const scopeForSubmit = scopeCardForSubmit ? scopeCardForSubmit.dataset.scope : 'all';
